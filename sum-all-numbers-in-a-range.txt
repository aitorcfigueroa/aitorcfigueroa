function sumAll(arr) {
  let newArr = 0;
  if (arr[0] < arr[1]) {
    for (let i = arr[0]; i < arr[1] + 1; i++) {
    newArr += i;
  }} else {
    for (let i = arr[0]; i > arr[1] - 1; i--) {
    newArr += i;
  }};
  return newArr;
}

sumAll([1, 4]);